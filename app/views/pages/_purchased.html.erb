<table class="table table-condensed"> 
			<thead> 
				<tr>   
					<th>Listing Name</th> 
					<th>Date</th>
					
				</tr> 
			</thead> 
			<tbody> 
				<% @purchased.each do |listing| %> 
				<tr> 
					<td><%= link_to sale.listing.name, pickup_path(sale.guid) %></td> 
					<td><%= time_ago_in_words(sale.created_at) %> ago 
				</tr> 
				<% end %>
		 </tbody> 
	 </table>